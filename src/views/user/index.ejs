<!--
Author: W3layouts
Author URL: http://w3layouts.com
License: Creative Commons Attribution 3.0 Unported
License URL: http://creativecommons.org/licenses/by/3.0/
-->
<!DOCTYPE html>
<html>
  <head>
    <% include ../shared/user/styles.ejs %>
  </head>
  <body>
    <% include ../shared/user/header.ejs %>
    <div id="vide">
      <div class="container">
        <div class="banner-info">
          <div class="dump-height"></div>
        </div>
      </div>
    </div>

    <!--content-->
    <div class="product">
      <div class="container">
        <div class="spec">
          <h3>Danh mục được quan tâm nhiều nhất</h3>
          <div class="ser-t">
            <b></b>
            <span><i></i></span>
            <b class="line"></b>
          </div>
        </div>
        <%if (categoryCollections && categoryCollections.length > 0) { %> <%
        categoryCollections.forEach(item => { %>
        <div class="col-md-4 m-w3ls">
          <div class="col-md1 fixed-height-320 card-category">
            <a href="/<%= item.dataValues.productTypeSlug %>/<%= item.slug %>">
              <img
                src="<%= item.dataValues.image %>"
                class="img-responsive img img-300"
                alt="<%= item.name %>"
                title="<%= item.name %>"
              />
            </a>
            <h3 class="most-cageogry-title">
              <a
                href="/<%= item.dataValues.productTypeSlug %>/<%= item.slug %>"
                class="linkkk"
              >
                <%= item.name %></a
              >
            </h3>
          </div>
        </div>
        <% }) %> <% } %>
      </div>
    </div>
    
    <!--content-->
    <div class="product" style="background-color: #fcfcff;">
      <div class="container">
        <div class="spec">
          <h3>Sản Phẩm Mới</h3>
          <div class="ser-t">
            <b></b>
            <span style="background-color: #fcfcff;"><i></i></span>
            <b class="line"></b>
          </div>
        </div>
        <div class="con-w3l">
		    <% newProducts.forEach(item => { %>
          <div class="col-md-3 pro-1">
            <div class="col-m fixed-height">
              <a href="/<%= item.slug %>" class="offer-img">
                <img
                  src="<%= item.image %> "
                  class="img-responsive img-110"
                  alt="<%= item.name %>"
                  title="<% item.name %>"
                />
              </a>
              <div class="mid-1">
                <div class="women">
                  <h6>
                    <a href="/<%= item.slug %>">
                      <% if (item.name.split(" ").length > 15) { %> <%=
                      item.name.slice(0, 100) %> ... <% } else { %> <%=
                      item.name %> <% } %>
                    </a>
                  </h6>
                </div>
                <% if (item.categories.id !== 12){ %>
                <div class="pr-single">
                  <p class="reduced">
                    Giá: Liên Hệ 
                    <% if (item.warranty > 0 ) { %>
                      &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp;
                      Bảo hành: <%= item.warranty %> Tháng
                    <% } %>
                  </p>
                </div>
                <% if (item.provider.id !== 26) { %>
                <p>
                  <strong>Thương hiệu: </strong>
                  <a
                    href="/<%= item.product_type.slug %>/<%= item.categories.slug %>/<%= item.provider.slug %>"
                    ><%= item.provider.name %></a
                  >
                </p>
                <% } %>
                <p>
                  <strong>Danh mục:</strong>
                  <a href="/<%= item.product_type.slug %>/<%= item.categories.slug %>"
                    ><%= item.categories.name %></a
                  >
                </p>
                <p>
                  <strong>Loại:</strong>
                  <a href="/<%= item.product_type.slug %>"
                    ><%= item.product_type.name %></a
                  >
                </p>
                <% } else { %>
                <div class="pr-single">
                  <p class="reduced">
                    Đăng lúc: <%= item.createdAt.toLocaleString() %>
                    <br />
                    Danh mục: <%= item.categories.name %>
                  </p>
                </div>
                <% } %>
              </div>
            </div>
          </div>
		    <% }) %>
          <div class="clearfix"></div>
        </div>
      </div>
	</div>
	
    <div class="product" style="background-color: #fbfeff;">
      <div class="container">
        <div class="spec">
          <h3>Sản Phẩm Bán Chạy Nhất</h3>
          <div class="ser-t">
            <b></b>
            <span style="background-color: #fbfeff;"><i></i></span>
            <b class="line"></b>
          </div>
        </div>
        <div class="con-w3l">
        <% topSellProducts.forEach(item => { %>
          <div class="col-md-3 pro-1">
            <div class="col-m fixed-height">
              <a href="/<%= item.slug %>" class="offer-img">
                <img
                  src="<%= item.image %> "
                  class="img-responsive img-110"
                  alt="<%= item.name %>"
                  title="<% item.name %>"
                />
              </a>
              <div class="mid-1">
                <div class="women">
                  <h6>
                    <a href="/<%= item.slug %>">
                      <% if (item.name.split(" ").length > 15) { %> <%=
                      item.name.slice(0, 100) %> ... <% } else { %> <%=
                      item.name %> <% } %>
                    </a>
                  </h6>
                </div>
                <% if (item.categories.id !== 12){ %>
                <div class="pr-single">
                  <p class="reduced">
                    Giá: Liên Hệ 
                    <% if (item.warranty > 0 ) { %>
                      &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp;
                      Bảo hành: <%= item.warranty %> Tháng
                    <% } %>
                  </p>
                </div>
                <% if (item.provider.id !== 26) { %>
                <p>
                  <strong>Thương hiệu: </strong>
                  <a
                    href="/<%= item.product_type.slug %>/<%= item.categories.slug %>/<%= item.provider.slug %>"
                    ><%= item.provider.name %></a
                  >
                </p>
                <% } %>
                <p>
                  <strong>Danh mục:</strong>
                  <a href="/<%= item.product_type.slug %>/<%= item.categories.slug %>"
                    ><%= item.categories.name %></a
                  >
                </p>
                <p>
                  <strong>Loại:</strong>
                  <a href="/<%= item.product_type.slug %>"
                    ><%= item.product_type.name %></a
                  >
                </p>
                <% } else { %>
                <div class="pr-single">
                  <p class="reduced">
                    Đăng lúc: <%= item.createdAt.toLocaleString() %>
                    <br />
                    Danh mục: <%= item.categories.name %>
                  </p>
                </div>
                <% } %>
              </div>
            </div>
          </div>
        <% }) %>
			  <div class="clearfix"></div>
		</div>
      </div>
	</div>
	
    <div class="product" style="background-color: #fcfcff;">
      <div class="container">
        <div class="spec">
          <h3>Sản Phẩm Được Quan Tâm Nhiều Nhất</h3>
          <div class="ser-t">
            <b></b>
            <span style="background-color: #fcfcff;"><i></i></span>
            <b class="line"></b>
          </div>
        </div>
        <div class="con-w3l">
        <% topViewProducts.forEach(item => { %>
          <div class="col-md-3 pro-1">
            <div class="col-m fixed-height">
              <a href="/<%= item.slug %>" class="offer-img">
                <img
                  src="<%= item.image %> "
                  class="img-responsive img-110"
                  alt="<%= item.name %>"
                  title="<% item.name %>"
                />
              </a>
              <div class="mid-1">
                <div class="women">
                  <h6>
                    <a href="/<%= item.slug %>">
                      <% if (item.name.split(" ").length > 15) { %> <%=
                      item.name.slice(0, 100) %> ... <% } else { %> <%=
                      item.name %> <% } %>
                    </a>
                  </h6>
                </div>
                <% if (item.categories.id !== 12){ %>
                <div class="pr-single">
                  <p class="reduced">
                    Giá: Liên Hệ 
                    <% if (item.warranty > 0 ) { %>
                      &nbsp; &nbsp; &nbsp; &nbsp; &nbsp; &nbsp;
                      Bảo hành: <%= item.warranty %> Tháng
                    <% } %>
                  </p>
                </div>
                <% if (item.provider.id !== 26) { %>
                <p>
                  <strong>Thương hiệu: </strong>
                  <a
                    href="/<%= item.product_type.slug %>/<%= item.categories.slug %>/<%= item.provider.slug %>"
                    ><%= item.provider.name %></a
                  >
                </p>
                <% } %>
                <p>
                  <strong>Danh mục:</strong>
                  <a href="/<%= item.product_type.slug %>/<%= item.categories.slug %>"
                    ><%= item.categories.name %></a
                  >
                </p>
                <p>
                  <strong>Loại:</strong>
                  <a href="/<%= item.product_type.slug %>"
                    ><%= item.product_type.name %></a
                  >
                </p>
                <% } else { %>
                <div class="pr-single">
                  <p class="reduced">
                    Đăng lúc: <%= item.createdAt.toLocaleString() %>
                    <br />
                    Danh mục: <%= item.categories.name %>
                  </p>
                </div>
                <% } %>
              </div>
            </div>
          </div>
        <% }) %>
			  <div class="clearfix"></div>
		</div>
      </div>
    </div>

    <div class="product" style="background-color: #fcfcff;">
      <div class="container">
        <div class="spec">
          <h3>Tin Tức Mới</h3>
          <div class="ser-t">
            <b></b>
            <span style="background-color: #fcfcff;"><i></i></span>
            <b class="line"></b>
          </div>
        </div>
        <div class="con-w3l autoplay">
          <% news.forEach(item => { %>
            <div class="col-md-4 m-w3ls">
              <div class="col-md1 fixed-height-320">
                <a href="/<%= item.slug %>">
                  <img src="<%= item.image %>" class="img-responsive img img-300" alt="<%= item.name %>" title="<%= item.name %>"/>
                  <div class="big-sa">
                    <h6><%= item.name %></h6>
                    <p>
                      <%- item.name %>...
                    </p>
                  </div>
                </a>
              </div>
            </div>
            <% }) %>
		</div>
      </div>
    </div>

    <!--footer-->
    <% include ../shared/user/footer.ejs %>
    <!-- //footer-->
    <% include ../shared/user/scripts.ejs %>
    <!-- smooth scrolling -->
            <div class="clearfix"></div>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
